# Sentinel Environment Configuration
# Copy this file to .env and update the values

# ============================================
# Database Configuration
# ============================================
DB_PASSWORD=sentinel_password
POSTGRES_PORT=5432

# ============================================
# JWT Configuration
# ============================================
# IMPORTANT: Change this in production!
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ============================================
# Logging
# ============================================
# Options: debug, info, warn, error
LOG_LEVEL=info

# ============================================
# Agent Configuration (Optional)
# ============================================
# Only needed if running the agent container
# Get the API key from the dashboard after creating a validator

# Unique identifier for this agent
AGENT_ID=demo-agent-1

# API key from dashboard (generated when you create a validator)
AGENT_API_KEY=demo_api_key_1

# Your Ethereum beacon node URL
# Use host.docker.internal to access services on the host machine
BEACON_NODE_URL=http://host.docker.internal:5052

# How often to check validator health (milliseconds)
HEALTH_CHECK_INTERVAL=30000

# Minimum number of agents required to confirm failure
CONSENSUS_THRESHOLD=2

# ============================================
# Notes
# ============================================
# 1. Never commit the .env file to git (it's in .gitignore)
# 2. Change JWT_SECRET to a random string in production
# 3. Use strong passwords for DB_PASSWORD
# 4. The agent container is optional - you can deploy agents separately
